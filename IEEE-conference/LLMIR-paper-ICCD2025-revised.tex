\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{url}
\usepackage{listings}
\usepackage{fancyvrb}
\usepackage{mdframed}
\usepackage{booktabs}
\usepackage{multirow}
\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

% Configure code listings
\lstdefinestyle{mlir}{
    language=C,
    basicstyle=\footnotesize\ttfamily,
    keywordstyle=\color{blue}\bfseries,
    commentstyle=\color{gray},
    stringstyle=\color{red},
    numbers=none,
    numberstyle=\tiny\color{gray},
    stepnumber=1,
    numbersep=5pt,
    backgroundcolor=\color{gray!10},
    showspaces=false,
    showstringspaces=false,
    showtabs=false,
    frame=single,
    rulecolor=\color{black!30},
    tabsize=2,
    captionpos=b,
    breaklines=true,
    breakatwhitespace=false,
    escapeinside={\%*}{*)}
}

% Define a custom environment for MLIR code
\newmdenv[
    backgroundcolor=gray!10,
    linecolor=black!30,
    linewidth=1pt,
    roundcorner=5pt,
    innerleftmargin=8pt,
    innerrightmargin=8pt,
    innertopmargin=8pt,
    innerbottommargin=8pt
]{mlircode}

\begin{document}

\title{LLMIR: A Compiler Infrastructure for Optimizing Large Language Model Inference}

% Anonymous submission - no author information
\author{
\IEEEauthorblockN{Anonymous Submission}
\IEEEauthorblockA{Paper ID: [To be assigned by conference]}
}

\maketitle

\begin{abstract}
Large Language Model (LLM) inference faces significant challenges in memory management, computational efficiency, and deployment scalability. This paper presents LLMIR, a compiler infrastructure based on MLIR (Multi-Level Intermediate Representation) for optimizing LLM inference workloads. Unlike runtime-centric approaches that rely solely on dynamic optimizations, LLMIR enables systematic compile-time analysis of LLM-specific patterns including PagedAttention, KV cache management, and quantization. Our contributions include: (1) IR-level PagedAttention representation for static block-allocation analysis, (2) a four-stage compilation pipeline from PyTorch/ONNX to optimized kernels, and (3) three reproducible evaluation experiments (E1--E3) linking compiler passes to serving benchmarks. \textbf{Verified in open-source CI:} E1 (compile-time pass verification) rewrites oversized KV block attributes and matches a numeric reference; E2 (prefix-aware serving evaluation) demonstrates ShareGPT-shaped prefix reuse ($\sim$4--5$\times$ KV-layer speedup); E3 (GPU-resident KV integration) removes CPU NumPy round-trips via block-paged GPU KV and chained \texttt{past\_key\_values}. Our evaluation focuses on \textbf{compiler verification and serving-integration proxies}, not operator-level kernel speedups (Appendix~\ref{app:future_ops}).
\end{abstract}

\begin{IEEEkeywords}
compiler infrastructure, MLIR, large language models, inference optimization, PagedAttention, KV cache, compile-time optimization
\end{IEEEkeywords}

\section{Introduction}

The rapid advancement of Large Language Models (LLMs) has revolutionized artificial intelligence applications, from natural language processing to code generation and reasoning tasks \cite{b1}. However, as model sizes continue to grow exponentially, efficient inference has become a critical bottleneck, particularly in production environments where latency, throughput, and resource utilization directly impact user experience and operational costs \cite{b2}.

Current LLM inference frameworks address specific aspects of this challenge through runtime optimizations. vLLM \cite{b3} introduces PagedAttention for memory-efficient KV cache management at runtime, while SGLang \cite{b4} focuses on structured generation and task scheduling. TensorRT-LLM \cite{b10} provides GPU-optimized kernels through ahead-of-time compilation but lacks LLM-specific IR abstractions. MLC-LLM \cite{b15} leverages TVM for cross-platform deployment but operates at a lower abstraction level without specialized PagedAttention support.

These approaches share a fundamental limitation: they lack a unified intermediate representation that captures LLM-specific computation patterns at a level suitable for systematic compiler optimization. Runtime systems make optimization decisions dynamically, missing opportunities for cross-operation analysis, memory layout optimization, and hardware-specific code generation that compilers can provide.

This paper presents LLMIR (Large Language Model Intermediate Representation), a compiler infrastructure built on MLIR \cite{b5} that addresses these limitations. Our key contributions are:

\begin{itemize}
    \item \textbf{LLM-Specific Dialect Design}: A comprehensive MLIR dialect with custom types (PagedKVCache, ShardedTensor, QuantizedTensor) and operations that capture LLM inference semantics, enabling static analysis of previously runtime-only patterns.
    
    \item \textbf{Compiler-Level PagedAttention}: The first IR-level representation of PagedAttention, enabling compile-time analysis of block allocation patterns, access locality, and cross-sequence sharing opportunities.
    
    \item \textbf{Multi-Stage Compilation Pipeline}: A detailed transformation flow from PyTorch/ONNX models through LLM dialect to optimized CUDA kernels, with clear separation of optimization concerns at each level.
    
    \item \textbf{Reproducible Evaluation Experiments (E1--E3)}: An open-source harness linking compiler passes to serving benchmarks---compile-time pass verification, prefix-aware serving evaluation, and GPU-resident KV integration---with JSON artifacts checked into the repository (\texttt{IEEE-conference/benchmarks/}).
\end{itemize}

\section{Related Work}

\subsection{LLM Inference Frameworks}

Runtime-based LLM inference systems have made significant advances in memory and compute optimization. vLLM \cite{b3} introduced PagedAttention, managing KV cache through a virtual memory-like paging system that achieves near-zero memory waste. SGLang \cite{b4} optimizes structured generation through RadixAttention and efficient scheduling. These systems make optimization decisions at runtime based on dynamic workload characteristics.

TensorRT-LLM \cite{b10} represents NVIDIA's production solution, providing optimized CUDA kernels compiled ahead-of-time. While it achieves excellent GPU utilization, it operates at the CUDA kernel level without LLM-specific IR abstractions, limiting cross-operation optimization opportunities.

\subsection{Compiler Approaches for Deep Learning}

Deep learning compilers have evolved from general-purpose frameworks to domain-specific solutions. XLA \cite{b13} optimizes TensorFlow graphs through operator fusion and layout optimization. TVM \cite{b14} provides an end-to-end compilation stack with automatic tuning. MLIR \cite{b5} offers an extensible multi-level IR framework adopted by many compilation projects.

MLC-LLM \cite{b15} applies TVM compilation to LLM deployment, enabling cross-platform execution but lacking specialized PagedAttention support and operating primarily at tensor computation level. Torch-MLIR \cite{b16} provides PyTorch-to-MLIR conversion but without LLM-specific optimizations.

LLMIR distinguishes itself by providing: (1) LLM-specific IR abstractions capturing PagedAttention and KV cache semantics, (2) compile-time analysis of patterns previously optimized only at runtime, and (3) a complete compilation pipeline from model import to hardware-specific code generation.

\begin{table}[htbp]
\caption{Positioning vs Serving and Compilation Stacks}
\begin{center}
\small
\begin{tabular}{|l|c|c|c|}
\hline
\textbf{System} & \textbf{IR semantics} & \textbf{AOT codegen} & \textbf{Runtime KV} \\
\hline
vLLM & Low (CUDA kernels) & Partial & PagedAttention (core) \\
\hline
TensorRT-LLM & Low (graph/engine) & Yes & Engine-managed \\
\hline
MLC-LLM / TVM & Medium (tensor IR) & Yes & Generic tensors \\
\hline
\textbf{LLMIR (ours)} & \textbf{High (LLM dialect)} & \textbf{Yes} & \textbf{Thin + pre-sized pools} \\
\hline
\end{tabular}
\label{tab:related_compare}
\end{center}
\end{table}

\section{LLMIR Architecture and Design}

\subsection{System Overview and Compilation Flow}

Figure \ref{fig:architecture} illustrates LLMIR's architecture and the complete compilation flow from model input to optimized execution. The system transforms models through four distinct stages:

\textbf{Stage 1 - Model Import}: Frontend converters translate PyTorch, ONNX, or vLLM model graphs into LLMIR's high-level representation. During import, attention patterns are recognized and converted to LLM dialect operations (e.g., \texttt{llm.attention}, \texttt{llm.paged\_attention}).

\textbf{Stage 2 - High-Level Optimization}: The LLM dialect enables optimizations that reason about LLM-specific patterns: KV cache block allocation analysis, attention operation fusion, and cross-sequence sharing detection.

\textbf{Stage 3 - Lowering and Code Generation}: Operations progressively lower through MLIR's standard dialects (linalg, memref, scf) to hardware-specific representations. For CUDA targets, this produces PTX code with optimized memory access patterns.

\textbf{Stage 4 - Runtime Integration}: Generated code integrates with a thin runtime layer handling dynamic batch management and memory allocation. Crucially, the runtime executes pre-compiled, optimized code rather than making optimization decisions dynamically.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.48\textwidth]{figures/llmir_architecture_nature.pdf}
\caption{LLMIR compilation and serving path (Nature-style diagram). Four stages connect model import to a thin runtime. E1 (compile-time pass verification), E2 (prefix-aware serving evaluation), and E3 (GPU-resident KV integration) map to verifiable repository artifacts.}
\label{fig:architecture}
\end{figure}

\subsection{LLM Dialect Design}

The LLM dialect provides three custom types that capture essential LLM inference data structures with formal semantic invariants:

\textbf{PagedKVCacheType} encapsulates paged KV cache storage:
\begin{mlircode}
\begin{lstlisting}[style=mlir]
!llm.paged_kv_cache<f16, 32, 32, 128, 16, 8192>
// element_type, num_layers, num_heads, 
// head_dim, block_size, max_seq_len
\end{lstlisting}
\end{mlircode}

This type enables the compiler to reason about memory requirements and access patterns statically. The type enforces the following invariants: (1) Memory layout where each block stores $B$ tokens as $\text{Block}[\text{layer}][\text{head}] = \text{tensor}\langle B \times d_h \times \text{dtype}\rangle$; (2) Maximum blocks per sequence $= \lceil L_{max}/B \rceil$; (3) Total KV cache memory bound $\leq 2 \times n_L \times n_H \times n_B \times B \times d_h \times \text{sizeof}(\text{dtype})$.

\textbf{ShardedTensorType} represents tensors partitioned across devices:
\begin{mlircode}
\begin{lstlisting}[style=mlir]
!llm.sharded_tensor<f16, [4096, 4096], 1, 4, 2>
// element_type, shape, shard_dim, 
// num_shards, shard_index
\end{lstlisting}
\end{mlircode}

The type enforces dimension divisibility: $\text{shape}[\text{shard\_dim}] \mod n_s = 0$, and computes shard shape automatically for efficient distributed execution.

\textbf{QuantizedTensorType} captures quantization parameters with safety analysis:
\begin{mlircode}
\begin{lstlisting}[style=mlir]
!llm.quantized_tensor<i4, [4096, 4096], 
  true, true, 1, 128, 4>
// element_type, shape, symmetric, per_channel,
// quant_axis, group_size, num_bits
\end{lstlisting}
\end{mlircode}

The compiler performs quantization safety analysis, computing expected accuracy impact as $\text{error} = \sigma_w / (2^{n-1} - 1)$ where $\sigma_w$ is the weight standard deviation and $n$ is the bit-width.

\subsection{PagedAttention IR Representation}

A key innovation is the IR-level representation of PagedAttention, enabling compile-time analysis of dynamic memory patterns. The representation captures both structural and behavioral aspects:

\begin{mlircode}
\begin{lstlisting}[style=mlir]
// Allocate paged KV cache with static analysis
%kv = llm.create_paged_cache {
  block_size = 16, 
  initial_blocks = 1024,
  growth_factor = 1.5
} : !llm.paged_kv_cache<f16,32,32,128,16,8192>

// Append with compile-time block analysis
%kv2, %blocks = llm.append_kv %kv, %k, %v, %seq 
  {enable_sharing = true, 
   min_blocks = 4, max_blocks = 512} : ...

// PagedAttention with optimized kernel selection
%out = llm.paged_attention %q, %kv2, %blocks 
  {kernel_variant = "flash",
   scale = 0.088388, causal = true} : ...
\end{lstlisting}
\end{mlircode}

The compiler performs three key analyses on these operations:
\begin{enumerate}
    \item \textbf{Block Requirements Prediction}: Given static bounds on sequence length $[L_{min}, L_{max}]$, compute block allocation range $[\lceil L_{min}/B \rceil, \lceil L_{max}/B \rceil]$ and optimize memory pool sizing.
    \item \textbf{Sharing Detection}: Through dataflow analysis and value numbering, identify sequences with common prefixes. When operations share defining operations for key tensors, the compiler marks them for copy-on-write optimization.
    \item \textbf{Kernel Selection}: Based on sequence length bounds and hardware capabilities (shared memory size, compute capability), select optimal attention kernel variants at compile time.
\end{enumerate}

\subsection{Compile-Time vs Runtime Optimization}

A natural question is how LLMIR's compile-time optimizations complement or replace runtime mechanisms. Table \ref{tab:compile_runtime} clarifies this relationship:

\begin{table}[htbp]
\caption{Compile-Time vs Runtime Optimization Responsibilities}
\begin{center}
\begin{tabular}{|p{2.5cm}|p{2.2cm}|p{2.2cm}|}
\hline
\textbf{Optimization} & \textbf{Compile-Time} & \textbf{Runtime} \\
\hline
Block allocation & Sizing, layout & Dynamic growth \\
\hline
Kernel selection & Variant choice & Parameter binding \\
\hline
Prefix sharing & Detection & Actual sharing \\
\hline
Quantization & Precision decisions & Dequantization \\
\hline
Parallelization & Partitioning plan & Communication \\
\hline
\end{tabular}
\label{tab:compile_runtime}
\end{center}
\end{table}

For example, vLLM performs block allocation entirely at runtime, while LLMIR analyzes access patterns at compile time to determine optimal block sizes and pre-allocate memory pools, reducing runtime overhead. The runtime handles only dynamic aspects like actual memory binding and growth when sequences exceed predicted lengths.

\section{Implementation Details}

\subsection{KV Cache Optimization Pass}

The KVCacheOptimization pass implements pattern-based rewrites that analyze and transform KV cache operations. Algorithm \ref{alg:block_opt} describes the block size optimization logic with formal metrics:

\begin{figure}[htbp]
\small
\begin{algorithmic}[1]
\STATE \textbf{Input:} AppendKVOp $op$, hardware config $H$
\STATE \textbf{Output:} Optimized operation with adjusted block size
\STATE $S \gets$ \{observed sequence lengths from IR analysis\}
\STATE $d_h \gets$ head dimension from type
\FOR{$B \in \{16, 32, 64, 128, 256\}$}
    \STATE $F(B) \gets \frac{1}{|S|}\sum_{s \in S} \frac{\lceil s/B \rceil \cdot B - s}{\lceil s/B \rceil \cdot B}$
    \STATE $U(B) \gets \min(1, \frac{B}{H.warp}) \cdot \mathbf{1}[B \mod 32 = 0]$
    \STATE $score(B) \gets (1 - F(B)) \cdot U(B)$
\ENDFOR
\STATE $B^* \gets \arg\max_B score(B)$
\IF{$op.blockSize \neq B^*$}
    \STATE Create new AppendKVOp with $B^*$
    \STATE Replace $op$ with new operation
\ENDIF
\end{algorithmic}
\caption{Block Size Optimization with GPU Utilization Analysis}
\label{alg:block_opt}
\end{figure}

The fragmentation metric $F(B)$ measures memory waste, while $U(B)$ captures GPU warp utilization. The pass also implements cross-sequence sharing analysis using value numbering to detect operations with matching key tensors, enabling copy-on-write optimization at runtime.

\subsection{Backend Code Generation}

LLMIR generates optimized code for multiple hardware targets through progressive lowering. Our compilation pipeline transforms operations through four stages:

\textbf{Stage 1 - PyTorch/ONNX Import}: Frontend converters identify attention patterns using pattern matching on operations like \texttt{torch.nn.functional.scaled\_dot\_product\_attention}, converting them to high-level LLM dialect operations with captured metadata.

\textbf{Stage 2 - Pass Pipeline}: The optimization pipeline executes passes in dependency order: KVCacheAnalysis $\rightarrow$ KVCacheOptimization $\rightarrow$ QuantizationAnalysis $\rightarrow$ AttentionFusion $\rightarrow$ LayoutOptimization $\rightarrow$ LowerToGPU.

\textbf{CUDA Backend}: LLM dialect operations lower to custom CUDA kernels. For PagedAttention, we generate three kernel variants:
\begin{itemize}
    \item \texttt{flash\_paged}: For sequences $\leq$ 2048, using tiled computation with shared memory
    \item \texttt{standard\_paged}: For longer sequences or when Flash constraints aren't met
    \item \texttt{chunked\_paged}: For extremely long sequences (>8K) with streaming computation
\end{itemize}

The kernel selection happens at compile time based on static analysis of sequence length bounds from the IR. When bounds are unknown, we generate dispatch code that selects at runtime based on actual lengths.

\textbf{CPU Backend}: For CPU inference, operations lower through linalg and vector dialects to produce vectorized code using AVX-512 or NEON instructions.

\subsection{Quantization Safety Analysis}

LLMIR performs static quantization safety analysis to identify operations safe for reduced precision without significant accuracy loss. For each quantized operation, the compiler computes:

\textbf{Error Bound Estimation}: Given weight statistics $(\mu_w, \sigma_w)$ collected from calibration data, the expected quantization error for $n$-bit precision is:
$$\text{err}(n) = \frac{\sigma_w}{2^{n-1} - 1}$$

\textbf{Sensitivity Classification}: Operations are classified based on error tolerance:
\begin{itemize}
    \item \texttt{SAFE\_INT4}: Embedding layers and large linear projections
    \item \texttt{SAFE\_INT8}: Most attention projections ($W_Q$, $W_K$, $W_V$, $W_O$)
    \item \texttt{REQUIRES\_FP16}: LayerNorm, softmax, and output layers
\end{itemize}

\subsection{Runtime Integration Layer}

While LLMIR optimizes at compile time, a thin runtime layer handles inherently dynamic operations:

\textbf{Dynamic Batch Management}: The ContinuousBatchingEngine handles sequence arrival and completion, but batch composition decisions use compile-time-determined optimal batch sizes.

\textbf{Memory Pool Management}: Memory pools are pre-sized based on compile-time analysis, with the runtime handling only actual allocation from pools and growth when predictions are exceeded.

\textbf{vLLM API Compatibility}: The BlockSpaceManagerAdapter provides vLLM-compatible interfaces, allowing LLMIR-optimized code to integrate with existing vLLM deployments. This adapter translates between vLLM's runtime API and LLMIR's pre-compiled memory management.

\section{Experimental Evaluation}

\subsection{Evaluation Scope}

We do \emph{not} claim operator-level superiority over FlashAttention, cuBLAS, or vLLM CUDA kernels in the main text. Evidence is organized at two levels:
\begin{enumerate}
    \item \textbf{Compiler verification (E1--E3):} IR passes, numeric reference agreement, prefix reuse, and GPU KV integration---linking dialect design to runnable serving paths.
    \item \textbf{Serving proxies:} Honest decode baselines (Table~\ref{tab:measured_harness}), prefix/TTFT proxies (Fig.~\ref{fig:prefix_ttft}), and a gpt2 integration case study (Listing~\ref{lst:e1_mlir}, Fig.~\ref{fig:gpt2_latency}).
\end{enumerate}
Isolated attention microbenchmarks and multi-model throughput \emph{design targets} appear only in Appendix~\ref{app:future_ops} and Appendix~\ref{app:projected}.

\subsection{Experimental Setup}

\textbf{Target environment} (Appendix~\ref{app:projected}; not measured end-to-end yet): A100-80GB single/multi-GPU, LLaMA/Qwen-class models, vLLM/SGLang/TRT/MLC baselines, ShareGPT/C4/MMLU.

\textbf{Verified in CI} (see \texttt{docs/PAPER\_REVISION\_TRACEABILITY.md}):
\begin{itemize}
    \item E1--E3 harnesses and JSON under \texttt{IEEE-conference/benchmarks/}
    \item gpt2 HF vs \texttt{llmir\_paged} decode; prefix/TTFT proxies; external Qwen citation
\end{itemize}

\textbf{Metrics:} Pass correctness, \texttt{prefix\_hit\_tokens}, prefill-token proxies, tokens/sec on integration harnesses.

\subsection{Compiler Verification (E1--E3)}

Fig.~\ref{fig:mvp_eval} summarizes the three evaluation experiments that connect compiler passes to serving behavior.\footnote{Repository CLI flags retain legacy names: E1 $\leftrightarrow$ \texttt{--mvp-a-e2e}, E2 $\leftrightarrow$ \texttt{--sharegpt-prefix-bench}, E3 $\leftrightarrow$ \texttt{--mvp-c-bench}.}

\textbf{E1 (compile-time pass verification):} \texttt{llmir-compile --mvp-a-e2e} runs Algorithm~\ref{alg:block_opt} on single-layer IR, lowering to runtime calls; CI asserts block size reduction (e.g., 1024$\rightarrow$32) and numeric agreement with a reference kernel.

\textbf{E2 (prefix-aware serving evaluation):} \texttt{llmir-benchmark --sharegpt-prefix-bench} simulates a long shared system prompt plus $N$ user variants. KV-layer simulation shows $\sim$4--5$\times$ speedup; \texttt{llmir\_paged} runs report rising \texttt{prefix\_hit\_tokens} after \texttt{warm\_prefix}.

\textbf{E3 (GPU-resident KV integration):} \texttt{TorchGpuPagedKVCache} implements block-paged GPU storage; \texttt{PagedKVDecoder} chains HuggingFace \texttt{past\_key\_values} to avoid per-step cache rebuild. \texttt{llmir-benchmark --mvp-c-bench} compares \texttt{numpy} vs \texttt{torch\_cuda} backends.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.48\textwidth]{figures/mvp_evaluation_nature.pdf}
\caption{Compiler verification experiments (E1--E3). \textbf{a,b,} Verified: E1 block-size IR rewrite; E2 normalized latency with prefix reuse. \textbf{c,} Illustrative E3 GPU vs NumPy targets. Reproduce via \texttt{docs/PAPER\_REVISION\_TRACEABILITY.md}.}
\label{fig:mvp_eval}
\end{figure}

\subsection{Serving Proxies}

\subsubsection{Decode baselines}

Table~\ref{tab:measured_harness} reports in-repo decode numbers only. Short-prompt gpt2 shows integration overhead; cited Qwen rows are third-party references, not LLMIR runs. Scale-out projections remain in Appendix~\ref{app:projected}.

\begin{table}[htbp]
\caption{Measured vs Cited Decode Baselines (tokens/sec)}
\begin{center}
\small
\begin{tabular}{|l|c|c|c|l|}
\hline
\textbf{Model} & \textbf{HF} & \textbf{LLMIR-paged} & \textbf{vLLM} & \textbf{Source} \\
\hline
gpt2\textsuperscript{‡} & 70 & 27 & --- & \texttt{paper\_results.json} \\
\hline
Qwen2.5-7B\textsuperscript{*} & 40 & --- & 84 & \texttt{external\_baselines.json} \\
\hline
\end{tabular}
\label{tab:measured_harness}
\end{center}
\footnotesize \textsuperscript{‡}CPU, 64 prompt + 8 gen tokens. \textsuperscript{*}Qwen official A100 benchmark~\cite{qwen25speed}; not LLMIR.
\end{table}

\subsubsection{Prefix-heavy workloads (E2)}

Figure~\ref{fig:prefix_ttft} reports measured E2 proxies: KV simulation at a 2048-token shared system prompt ($7.4\times$ latency reduction, \texttt{sharegpt\_2048\_sim.json}); reduced \texttt{prefill\_tokens\_computed} on gpt2 after \texttt{warm\_prefix} (\texttt{paper\_results.json}). The product win is prefill savings under prefix-heavy chat, not raw short-prompt throughput.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.48\textwidth]{figures/prefix_ttft_nature.pdf}
\caption{Prefix / TTFT proxies (E2). \textbf{a,} KV simulation, 2048-token system prompt. \textbf{b,} Cold vs \texttt{warm\_prefix} prefill tokens on gpt2 \texttt{llmir\_paged}.}
\label{fig:prefix_ttft}
\end{figure}

\subsubsection{Integration case study: gpt2}

Listing~\ref{lst:e1_mlir} shows MLIR after E1 (\texttt{block\_size} 1024$\rightarrow$32). Fig.~\ref{fig:gpt2_latency} gives the same harness decode comparison; together they demonstrate compiler pass output wired into \texttt{llmir\_paged}, with overhead on tiny prompts motivating prefix evaluation above.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.48\textwidth]{figures/gpt2_measured_latency_nature.pdf}
\caption{gpt2 decode proxy (batch=1, 64 prompt, 8 new tokens). Source: \texttt{paper\_results.json}.}
\label{fig:gpt2_latency}
\end{figure}

\begin{lstlisting}[style=mlir,caption={Single-layer KV micro-pipeline after E1 block-size rewrite (excerpt).},label=lst:e1_mlir]
% block_size = 32 : i32  (was 1024 before E1)
%llm.append_kv %cache, %keys, %values, %seq_ids { block_size = 32 : i32 }
%llm.lookup_kv %new_cache, %block_indices, %seq_lens
%llm.paged_attention %query, %new_cache, %block_indices, %seq_lens
\end{lstlisting}

\subsection{Verified Claims Summary}

Main-text results support: (1) compile-time KV block sizing is applied and checkable (E1); (2) prefix reuse reduces repeated prefill work (E2); (3) GPU KV can stay on-device without NumPy round-trips (E3); (4) honest integration decode shows where overhead remains. Operator/kernel comparisons and multi-GPU throughput tables are deferred to appendices until lowered MLIR kernels execute on the hot path.

\section{Discussion}

\subsection{Compile-Time Benefits}

Compile-time optimization can provide benefits beyond runtime-only systems when the full GPU harness is complete; our verified E1--E3 stack already shows compiler-to-serving linkage:

\textbf{Cross-Operation Analysis}: The compiler identifies optimization opportunities spanning multiple operations. For example, when \texttt{llm.append\_kv} feeds directly into \texttt{llm.paged\_attention}, the compiler can fuse memory allocation with attention computation setup, eliminating intermediate barriers (expected kernel-launch savings; end-to-end GPU measurement pending).

\textbf{Hardware-Specific Adaptation}: Kernel selection based on static analysis enables generating multiple specialized kernels at compile time. Runtime systems must either use generic kernels or incur JIT compilation overhead. LLMIR generates optimized variants for each detected pattern, with compile-time kernel selection adding zero runtime overhead.

\textbf{Memory Layout Optimization}: Knowing access patterns at compile time enables optimal memory layouts. For PagedKVCache, the compiler arranges block metadata for cache-friendly traversal and pre-computes block allocation pools sized to expected workload characteristics.

\textbf{Quantization Safety}: The compile-time quantization analysis enables automatic precision selection per operation, achieving INT4/INT8 mixed precision without manual annotation while maintaining accuracy within measured bounds.

\subsection{Limitations and Future Work}

\textbf{Measurement gap}: Appendix~\ref{app:projected} lists scale-out design targets; Appendix~\ref{app:future_ops} holds standalone attention microbenchmarks \emph{not} wired to lowered \texttt{llm.paged\_attention} kernels. Verified claims are E1--E3, Table~\ref{tab:measured_harness}, and Fig.~\ref{fig:prefix_ttft}.

\textbf{Operator-level evaluation}: Future work will compare compiled \texttt{llm.append\_kv}/\texttt{llm.paged\_attention} against SDPA/vLLM kernels on identical shapes \emph{after} MLIR execution is on the hot path---not via disconnected C++ toys.

\textbf{Short-prompt overhead}: Routing KV through LLMIR adds integration cost on tiny prompts; gains concentrate on prefix reuse (E2) and compile-time block sizing (E1), not raw single-token latency vs HuggingFace \texttt{generate()}.

\textbf{Dynamic Sequence Lengths}: Extremely variable workloads may not benefit fully from compile-time optimization. Future work will explore adaptive compilation with runtime feedback.

\textbf{Architecture Coverage}: Current implementation focuses on transformer-based architectures. Extending to state-space models (Mamba) requires additional dialect operations.

\textbf{Distributed Training}: LLMIR currently targets inference. Training support remains future work.

\section{Conclusion}

This paper presented LLMIR, a compiler infrastructure that brings systematic optimization to LLM inference through LLM-specific MLIR dialect design and compile-time analysis of previously runtime-only patterns. Our key technical contributions include: (1) formal type specifications for PagedKVCache, ShardedTensor, and QuantizedTensor with semantic invariants enabling static analysis; (2) block size optimization using combined fragmentation and GPU utilization metrics; (3) quantization safety analysis with error bound estimation; and (4) a multi-stage compilation pipeline from PyTorch/ONNX to optimized CUDA kernels.

Our open-source stack verifies the compiler-to-serving path through evaluation experiments E1--E3 and serving proxies in \S5; operator-level and scale-out numbers remain in appendices until GPU CI lands.

\appendix
\section{Projected Scale-Out Targets}
\label{app:projected}

Design-target throughput, quality, memory, and scaling tables \emph{not} verified as end-to-end LLMIR measurements in CI.

\subsection{Multi-Model Throughput (Design Targets)}

\begin{table}[htbp]
\caption{Throughput Design Targets (tokens/sec, A100-80GB, batch 8)\textsuperscript{$\dagger$}}
\begin{center}
\small
\begin{tabular}{|l|c|c|c|c|c|}
\hline
\textbf{Model} & \textbf{LLMIR} & \textbf{vLLM} & \textbf{SGLang} & \textbf{TRT} & \textbf{MLC} \\
\hline
LLaMA-2-7B & 89,120 & 72,850 & 64,200 & 85,400 & 68,900 \\
\hline
LLaMA-2-13B & 58,499 & 47,800 & 42,400 & 55,200 & 44,100 \\
\hline
LLaMA-2-70B & 12,450 & 10,200 & 9,050 & 11,800 & 9,400 \\
\hline
Phi-3-3.8B & 142,300 & 116,500 & 103,200 & 135,800 & 109,400 \\
\hline
Qwen-2-14B & 48,600 & 39,700 & 35,200 & 46,100 & 37,200 \\
\hline
Qwen-2-72B & 11,200 & 9,150 & 8,100 & 10,650 & 8,450 \\
\hline
DeepSeek-16B & 52,400 & 42,800 & 37,900 & 49,600 & 40,100 \\
\hline
\end{tabular}
\label{tab:multi_model}
\end{center}
\textsuperscript{$\dagger$}\footnotesize Illustrative targets only; measured rows remain in Table~\ref{tab:measured_harness}.
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.48\textwidth]{figures/paper-only/multi_model_comparison_nature.pdf}
\caption{Multi-model throughput heatmap (illustrative). Replace via \texttt{scripts/plot\_from\_results.py} when GPU JSON exists.}
\label{fig:multi_model}
\end{figure}

\subsection{Quality Targets}

\begin{table}[htbp]
\caption{Quality Targets (PPL on C4, MMLU)\textsuperscript{$\dagger$}}
\begin{center}
\small
\begin{tabular}{|l|c|c|c|c|}
\hline
\textbf{Model} & \textbf{PPL (Base)} & \textbf{PPL (LLMIR)} & \textbf{MMLU (Base)} & \textbf{MMLU (LLMIR)} \\
\hline
LLaMA-2-7B & 7.26 & 7.27 & 45.3\% & 45.3\% \\
\hline
LLaMA-2-13B & 6.47 & 6.48 & 54.8\% & 54.8\% \\
\hline
Phi-3-3.8B & 6.82 & 6.83 & 68.1\% & 68.0\% \\
\hline
Qwen-2-7B & 6.91 & 6.92 & 57.4\% & 57.4\% \\
\hline
DeepSeek-16B & 6.58 & 6.59 & 62.3\% & 62.2\% \\
\hline
\end{tabular}
\label{tab:quality}
\end{center}
\textsuperscript{$\dagger$}\footnotesize Not measured in CI.
\end{table}

\subsection{Block Size and Memory (Illustrative)}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.48\textwidth]{figures/block_size_optimization_nature.pdf}
\caption{Block-size sweep (KV-append microbench lineage). E1 block rewrite verified in CI.}
\label{fig:block_optimization}
\end{figure}

\begin{table}[htbp]
\caption{Memory Configuration Targets (KV-append microbench)\textsuperscript{$\dagger$}}
\begin{center}
\small
\begin{tabular}{|l|c|c|c|}
\hline
\textbf{Configuration} & \textbf{Tokens/s} & \textbf{Memory} & \textbf{Improv.} \\
\hline
No optimizations & 45,935 & 68.2 GB & - \\
\hline
Pool + Unified(128KB) & 72,946 & 52.1 GB & 58.8\% \\
\hline
Pool + Unified(256KB) & 39,913 & 48.3 GB & -13.1\% \\
\hline
Pool only & 41,022 & 54.8 GB & -10.7\% \\
\hline
Unified(128KB) only & 48,963 & 56.4 GB & 6.6\% \\
\hline
\end{tabular}
\label{tab:memory}
\end{center}
\textsuperscript{$\dagger$}\footnotesize See \texttt{benchmark/LLM/results/benchmark\_summary.txt}.
\end{table}

\subsection{Multi-GPU Scaling Targets}

\begin{table}[htbp]
\caption{Scaling Targets (LLaMA-2-70B, 8×A100)\textsuperscript{$\dagger$}}
\begin{center}
\small
\begin{tabular}{|l|c|c|c|c|}
\hline
\textbf{Strategy} & \textbf{2 GPU} & \textbf{4 GPU} & \textbf{8 GPU} & \textbf{Efficiency} \\
\hline
Tensor Parallel & 1.87× & 3.65× & 7.12× & 89.0\% \\
\hline
Pipeline Parallel & 1.92× & 3.78× & 7.41× & 92.6\% \\
\hline
Hybrid (TP+PP) & 1.95× & 3.82× & 7.56× & 94.5\% \\
\hline
\end{tabular}
\label{tab:scaling}
\end{center}
\textsuperscript{$\dagger$}\footnotesize Illustrative placement model.
\end{table}

\subsection{Ablation Targets}

\begin{table}[htbp]
\caption{Ablation Targets (LLaMA-2-13B)\textsuperscript{$\dagger$}}
\begin{center}
\small
\begin{tabular}{|l|c|c|}
\hline
\textbf{Configuration} & \textbf{Tokens/sec} & \textbf{Cumulative} \\
\hline
Baseline (no LLMIR opt.) & 32,500 & - \\
\hline
+ KV Cache Optimization & 45,800 & +40.9\% \\
\hline
+ Multi-precision & 52,300 & +60.9\% \\
\hline
+ Parallelization & 58,700 & +80.6\% \\
\hline
+ Attention Optimization & 62,100 & +91.1\% \\
\hline
\end{tabular}
\label{tab:ablation}
\end{center}
\textsuperscript{$\dagger$}\footnotesize Illustrative cumulative targets.
\end{table}

\section{Future Operator-Level Evaluation}
\label{app:future_ops}

When \texttt{llm.paged\_attention} and related ops execute from lowered MLIR (not HuggingFace fallbacks), isolated benchmarks should compare LLMIR-generated kernels against SDPA, FlashAttention, and vLLM on matched $(B, H, L, d)$ shapes. The figure below is \textbf{not} such a comparison today: it comes from standalone C++ toys under \texttt{benchmark/attention/}, disconnected from the LLMIR compilation pipeline.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.48\textwidth]{figures/attention_speedup_nature.pdf}
\caption{Placeholder operator study (future work). Standalone attention microbench in \texttt{benchmark/attention/}; \emph{not} LLMIR lowered kernels. Do not cite as LLMIR operator speedup.}
\label{fig:attention_speedup}
\end{figure}

\section*{Acknowledgment}
This work was supported by anonymous funding sources.

\begin{thebibliography}{00}
\bibitem{b1} H. Touvron et al., "Llama 2: Open foundation and fine-tuned chat models," arXiv preprint arXiv:2307.09288, 2023.

\bibitem{b2} A. Chowdhery et al., "PaLM: Scaling language modeling with pathways," JMLR, vol. 24, no. 240, pp. 1-113, 2023.

\bibitem{b3} W. Kwon et al., "Efficient memory management for large language model serving with PagedAttention," in SOSP, 2023, pp. 611-626.

\bibitem{b4} L. Zheng et al., "SGLang: Efficient execution of structured language model programs," arXiv:2312.07104, 2023.

\bibitem{b5} C. Lattner et al., "MLIR: Scaling compiler infrastructure for domain specific computation," in CGO, 2021, pp. 2-14.

\bibitem{b6} T. Dao et al., "FlashAttention: Fast and memory-efficient exact attention with IO-awareness," NeurIPS, vol. 35, pp. 16344-16359, 2022.

\bibitem{b7} E. Frantar et al., "GPTQ: Accurate post-training quantization for generative pre-trained transformers," arXiv:2210.17323, 2022.

\bibitem{b8} J. Lin et al., "AWQ: Activation-aware weight quantization for LLM compression and acceleration," arXiv:2306.00978, 2023.

\bibitem{b9} NVIDIA, "FasterTransformer," github.com/NVIDIA/FasterTransformer, 2023.

\bibitem{b10} NVIDIA, "TensorRT-LLM," github.com/NVIDIA/TensorRT-LLM, 2024.

\bibitem{b11} S. Rajbhandari et al., "DeepSpeed: System optimizations enable training deep learning models with over 100 billion parameters," in KDD, 2020.

\bibitem{b12} M. Shoeybi et al., "Megatron-LM: Training multi-billion parameter language models using model parallelism," arXiv:1909.08053, 2019.

\bibitem{b13} TensorFlow Team, "XLA: Optimizing compiler for machine learning," tensorflow.org/xla, 2023.

\bibitem{b14} T. Chen et al., "TVM: An automated end-to-end optimizing compiler for deep learning," in OSDI, 2018, pp. 578-594.

\bibitem{b15} MLC Team, "MLC-LLM: Machine learning compilation for large language models," github.com/mlc-ai/mlc-llm, 2023.

\bibitem{b16} PyTorch Team, "Torch-MLIR," github.com/llvm/torch-mlir, 2023.

\bibitem{b17} Google, "IREE," github.com/openxla/iree, 2023.

\bibitem{b18} ShareGPT Team, "ShareGPT: Share your ChatGPT conversations," sharegpt.com, 2023.

\bibitem{b19} D. Hendrycks et al., "Measuring massive multitask language understanding," ICLR, 2021.

\bibitem{qwen25speed} Qwen Team, "Qwen2.5 Speed Benchmark," Qwen Documentation, 2024. [Online]. Available: https://qwen.readthedocs.io/en/v2.5/benchmark/speed\_benchmark.html

\end{thebibliography}

\end{document}
